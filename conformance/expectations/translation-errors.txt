# Guest failures proved to be owned by shader translation.
#
# One case name per line. Every active entry must name its ignored `bugs/`
# handoff package in the comment. The scorer fails if the case starts passing,
# because that means either the translator was repaired or the case stopped
# reaching the translated behavior.

# No active macos-15 conformance failure currently reaches translation.
#
# `bugs/compute-multisample-query-reports-one/` remains a confirmed latent
# translator defect, but the active `msaa4_color_store_no_resolve` case is
# driver-owned: this build refuses the multisample sampled image as
# `unstageable_multisampled` before translation can execute it.
